<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                   https://raw.github.com/doctrine/doctrine2/master/doctrine-mapping.xsd">

    <mapped-superclass name="DobryProgramator\SmartformBundle\Entity\AbstractSmartformAddress">
        <field name="code"/>

        <field name="latitude"/>

        <field name="longitude"/>

        <field name="street"/>

        <field name="houseNumber"/>

        <field name="orientationNumber" nullable="true"/>

        <field name="evidenceNumber" nullable="true"/>

        <field name="city"/>

        <field name="zipCode" length="5"/>

        <field name="district"/>

        <field name="region"/>

        <field name="country"/>
    </mapped-superclass>
</doctrine-mapping>
